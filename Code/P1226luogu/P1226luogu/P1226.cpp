#include <iostream>
#include <cstdio>
#include <cstdlib>

using namespace std;

long long _pow[1020] = { 0 };
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	int b, p, k;
	cin >> b >> p >> k;
	int cur = p;
	while (cur != 0) 
	{
		
	}

}